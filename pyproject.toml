[project]
name = "facebook-post-comment-scraper"
version = "0.1.0"
description = "Facebook post and comment scraper"
requires-python = ">=3.11"
dependencies = [
    "python-dotenv>=1.2.2",
    "requests>=2.33.0",
]

[tool.uv]
package = false
